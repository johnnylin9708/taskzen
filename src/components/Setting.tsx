import React from "react";
import Box from "@mui/material/Box";

const Setting = () => {
  return (
    <Box
      sx={{ width: "100%", height: "100vh", backgroundColor: "#000000" }}
    ></Box>
  );
};

export default Setting;
